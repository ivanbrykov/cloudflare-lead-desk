# Security policy

Please do not report vulnerabilities through public issues. Email `Ivan.ru@gmail.com` with a concise reproduction and impact assessment.

The alpha requires Cloudflare Access for staff routes. Integration tokens are write-only, scoped to intake, shown once, stored as SHA-256 hashes, and revocable from the UI.
