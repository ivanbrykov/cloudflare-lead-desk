{
  "devDependencies": {
    "wrangler": "4.125.0"
  },
  "engines": {"node": ">=24.20.0 <25"},
  "name": "lead-desk-upgrade-service",
  "packageManager": "pnpm@10.34.5",
  "private": true,
  "scripts": {
    "check": "pnpm run test && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test"
  },
  "type": "module"
}
